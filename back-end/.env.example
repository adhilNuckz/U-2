# Server
PORT=3000
NODE_ENV=development

# Database (MySQL on Server 1)
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=u2test
DB_PASSWORD=fuckoff
DB_DATABASE=u2test

# JWT
JWT_SECRET=your_super_secret_jwt_key_change_this
JWT_EXPIRES_IN=24h

# Docker (Server 2)
DOCKER_HOST= 
CONTAINER_MAX_LIFETIME=1800  # 30 minutes in seconds
CONTAINER_MEMORY_LIMIT=150m
CONTAINER_CPU_LIMIT=0.3

# Redis (for sessions)
REDIS_HOST=localhost
REDIS_PORT=6379

# Rate limiting
MAX_CONTAINERS_PER_USER=1